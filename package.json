{
  "name": "llm-observability-platform",
  "private": true,
  "workspaces": [
    "frontend",
    "ingestion",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=packages/sdk\" \"npm run dev --workspace=frontend\" \"npm run dev --workspace=ingestion\"",
    "build": "npm run build --workspace=packages/sdk && npm run build --workspace=frontend && npm run build --workspace=ingestion",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
